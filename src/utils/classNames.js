/**
 * classNames.js
 * Re-exports clsx as `cn` — the standard way to compose class strings
 * throughout this codebase. Import like: import { cn } from '@utils/classNames';
 */
export { clsx as cn } from 'clsx';
